<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
          http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.1.xsd">

    <security:global-method-security secured-annotations="enabled"/>

    <bean id="passwordEncoder"
          class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

    <security:http auto-config="true" authentication-manager-ref="myAuthenticationManager"
                   use-expressions="true"
                   entry-point-ref="authenticationEntryPoint">

        <security:intercept-url pattern="/login" access="permitAll" />
        <security:intercept-url pattern="/accessdenied" access="permitAll"/>

        <security:intercept-url pattern="/css/**" access="permitAll"/>
        <security:intercept-url pattern="/js/**" access="permitAll"/>
        <security:intercept-url pattern="/img/**" access="permitAll"/>


        <security:intercept-url pattern="/regions" access="hasRole('ROLE_USER')"/>
        <security:intercept-url pattern="/scriptList/**" access="hasRole('ROLE_USER')"/>
        <security:intercept-url pattern="/show/**" access="hasRole('ROLE_USER')"/>
        <security:intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')"/>

        <security:intercept-url pattern="/" access="isAuthenticated()"/>

        <!--должно идти последним пунктом, чтобы отрабатывали все остальные правила-->
        <security:intercept-url pattern="/**" access="isAuthenticated()"/>


        <security:logout logout-url="/logout"
                         invalidate-session="true"
                         logout-success-url="/login"
                         delete-cookies="JSESSIONID"/>

        <security:access-denied-handler ref="accessDeniedHandler"/>

        <security:remember-me key="sauronKey" user-service-ref="myAuthenticationProvider" token-validity-seconds="259200"/>

    </security:http>

    <bean id="accessDeniedHandler" class="ug.progress.monitoring.handler.AccessDeniedHandler">
        <property name="redirectUrl" value="/accessdenied"/>
    </bean>

    <bean id="authenticationEntryPoint"
          class="org.springframework.security.web.authentication.LoginUrlAuthenticationEntryPoint"
          p:loginFormUrl="/login" p:useForward="true"/>


    <bean id="logoutHandler" class="ug.progress.monitoring.handler.LogoutHandler"/>

    <bean id="myAuthenticationProvider" class="ug.progress.monitoring.manager.UserManager"/>

    <security:authentication-manager id="myAuthenticationManager">
        <security:authentication-provider ref="myAuthenticationProvider"/>
    </security:authentication-manager>

</beans>